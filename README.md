# Tracking_data_cleaner_shiny
Shiny app for cleaning tracking data with steps for reviewing flagged fixes and manually removing fixes.

# INSTRUCTIONS
1. Clone the repo (or download ZIP from GitHub)
2. Open the .Rproj file in RStudio
3. Restore packages
renv::restore()

4. Run the app
shiny::runApp()
